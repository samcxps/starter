{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "alchemy dev",
    "deploy:staging": "alchemy deploy --app api --stage staging",
    "deploy:production": "alchemy deploy --app api --stage production",
    "destroy:staging": "alchemy destroy --app api --stage staging",
    "destroy:production": "alchemy destroy --app api --stage production"
  },
  "dependencies": {
    "infra": "workspace:",
    "alchemy": "catalog:"
  },
  "devDependencies": {
    "@repo/config": "workspace:"
  }
}
